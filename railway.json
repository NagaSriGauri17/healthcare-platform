{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "./mvnw clean install -DskipTests -B"
  },
  "deploy": {
    "startCommand": "java -Dserver.port=$PORT -jar target/*.jar",
    "restartPolicyType": "ON_FAILURE"
  }
}
